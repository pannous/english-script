# circleci integration: https://circleci.com/gh/pannous/english-script
dependencies:
  bundler:
    without:
      - development
test:
  post:
    - bundle exec rake test
# machine:
#   ruby:
#     version:
